import type {ReactElement} from 'react';

export const Section1 = (): ReactElement => {
	return (
		<div className={'grid grid-cols-3 gap-x-6'}>
			<div className={'bg-table flex flex-col rounded-2xl p-10    '}>
				<div className={'leading-7xl font-[Aeonik sans] text-7xl font-black text-white'}>
					{'YEARN PARTNER VAULTS'}
				</div>
				<p>{'lol'}</p>
			</div>
		</div>
	);
};
